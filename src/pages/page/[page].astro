---
// Pagination was removed — the home feed shows ALL posts on one page
// (user preferred the un-paginated view). This route intentionally emits
// zero pages. Kept as an empty stub because the file cannot be deleted in
// this environment; it produces no /page/N output.
export function getStaticPaths() {
  return [];
}
---
